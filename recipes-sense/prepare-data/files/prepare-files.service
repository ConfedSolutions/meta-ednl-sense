[Unit]
Description=Prepare default files for /data and /security partitions
DefaultDependencies=no
After=data.mount security.mount partition-disk.service

[Service]
Type=oneshot
User=root
ExecStart=/usr/sbin/prepare-files
RemainAfterExit=yes

[Install]
WantedBy=sysinit.target
